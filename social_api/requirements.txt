fastapi==0.135.1
uvicorn[standard]==0.41.0
sqlalchemy==2.0.48
alembic==1.18.4
psycopg2-binary==2.9.11
pydantic==2.12.5
pydantic-settings==2.13.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.22
email-validator==2.3.0
bcrypt==5.0.0
jinja2==3.1.6
aiofiles==24.1.0
slowapi==0.1.9
Pillow==12.2.0
boto3==1.43.2
# google-auth[requests] verifies Google ID tokens server-side for "Sign in with
# Google". The [requests] extra pulls in `requests`, needed by the
# google.auth.transport.requests.Request transport (not a core google-auth dep).
google-auth[requests]==2.55.0
# CommonMark renderer for the help centre. Pure Python, no C extension; the
# one transitive dep is mdurl. Configured with html=False in
# services/help_render.py, which is what keeps a body from emitting raw HTML.
markdown-it-py==3.0.0
# Offline fallback classifier for text moderation — used when the primary
# provider is unreachable, so a vendor outage never leaves text unscanned.
# Pulls in scikit-learn; that weight is the price of a no-network fallback.
alt-profanity-check==1.6.1
